package com.pding.paymentservice.models.enums;

/**
 * Enum representing the type of video package.
 * THEME_PACKGE: Traditional package where users must purchase all videos in the package.
 * FREE_CHOICE_PACKAGE: Package where users can select specific videos to purchase.
 */
public enum PackageType {
    FREE_CHOICE_PACKAGE,
    THEME_PACKAGE
}
